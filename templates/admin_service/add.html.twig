{% extends 'baseAdmin.html.twig' %}

{% block body %}
    <h1>Ajouter un service</h1>

   <div class="container mt-4">
        <div class="row justify-content-center">
            <div class="col-12 col-sm-10 col-md-8 col-lg-6 col-xl-5">
                {# <h2 class="mb-4 text-center">Ajouter un service</h2> #}

                <div class="p-3">
{# 
                {% if service.image %}
    <p>Image actuelle :</p>
    <img src="{{ asset('uploads/' ~ service.image) }}" alt="Image du service" style="max-width: 200px;">
{% endif %}

{% if service.imageHeader %}
    <p>Image d'en-tête actuelle :</p>
    <img src="{{ asset('uploads/' ~ service.imageHeader) }}" alt="Header du service" style="max-width: 200px;">
{% endif %} #}
               

               {{ form_start(form, {'attr': {'enctype': 'multipart/form-data'}}) }}

                    {{ form_row(form.ImageHeader) }}
                    {{ form_row(form.title) }}
                    {{ form_row(form.image) }}
                    {{ form_row(form.description) }}
                    {{ form_row(form.price) }}
                    {{ form_row(form.paragraph) }}                 

                   
                    <div class="text-center mt-3">
                        <button type="submit" class="btn btn-contact mt-3">Enregistrer</button>
                    </div>

                {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}